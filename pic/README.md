Dependency exiftool

```
setdesc.py  : set/get exif EXIF:ImageDescription for adding commentary text to pictures
updateid.py : set EXIF:ImageUniqueID with md5 if not already present
sortpics.py : deduplicate picture collection and sort via calendar

```
